<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- Outside panal of buttons -->
    <item>
        <shape>
            <solid android:color="@color/outsidePanelClr" />

            <stroke
                android:width="0dp"
                android:color="@color/outsidePanelClr" />

            <corners android:radius="0dp" />

            <padding
                android:bottom="@dimen/btnOutsidePadding"
                android:left="@dimen/btnOutsidePadding"
                android:right="@dimen/btnOutsidePadding"
                android:top="@dimen/btnOutsidePadding" />
        </shape>
    </item>
    <!-- Button shadow -->
    <item>
        <shape>
            <solid android:color="@color/btnShadowClr" />

            <stroke
                android:width="0dp"
                android:color="@color/btnShadowClr" />

            <corners android:radius="@dimen/btnRadius" />

            <padding
                android:bottom="@dimen/btnShadowPadding"
                android:left="0dp"
                android:right="0dp"
                android:top="0dp" />
        </shape>
    </item>
    <!-- Main part of button -->
    <item>
        <shape>
            <solid android:color="@color/btnMainClr" />

            <stroke
                android:width="0dp"
                android:color="@color/btnMainClr" />

            <corners android:radius="@dimen/btnRadius" />

            <padding
                android:bottom="@dimen/btnMainPadding"
                android:left="0dp"
                android:right="@dimen/btnMainPadding"
                android:top="0dp" />
        </shape>
    </item>

</layer-list>